services:
  - type: web
    name: sniper-roulette-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT --workers 1 --loop uvloop --http h11"
